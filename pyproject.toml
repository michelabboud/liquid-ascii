[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "liquid-ascii"
version = "0.4.0"
description = "Terminal-based ASCII art animation with liquid 3D rendering, gestures, and lip sync"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Liquid ASCII Contributors"}
]
maintainers = [
    {name = "Liquid ASCII Contributors"}
]
keywords = [
    "ascii",
    "animation",
    "terminal",
    "tts",
    "lip-sync",
    "raymarching",
    "3d-rendering",
    "sdf",
    "visual-effects",
    "character-animation",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Artistic Software",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Multimedia :: Graphics :: 3D Rendering",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Topic :: Terminals",
]
dependencies = [
    "blessed>=1.49.0",
    "numpy>=2.4.6",
    "edge-tts>=7.2.8",
    "sounddevice>=0.5.6",
    "scipy>=1.17.1",
    "aiohttp>=3.14.3",
    "requests>=2.34.2",
]

[project.urls]
Homepage = "https://github.com/anthropics/liquid-ascii"
Documentation = "https://github.com/anthropics/liquid-ascii/blob/main/README.md"
Repository = "https://github.com/anthropics/liquid-ascii"
Issues = "https://github.com/anthropics/liquid-ascii/issues"
Changelog = "https://github.com/anthropics/liquid-ascii/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.6",
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
]
test = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
]
lint = [
    "ruff>=0.16.6",
    "mypy>=2.3.1",
]

[project.scripts]
liquid-ascii = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
